# c64-https ld65 config — UCI (Ultimate Command Interface) backend
#
# PLACEHOLDER / TODO — not yet functional.
#
# Intended target: Commodore Ultimate 64 / U64E, using UCI ethernet
# instead of ip65 + RR-Net. Most MEMORY/SEGMENTS should mirror the
# ip65 cfg; the NET_* regions will differ once the UCI backend lands
# under src/net/uci/.
#
# This file exists to reserve the filename and document intent.
# Do NOT build with BACKEND=uci yet — it will fail.
